# Copia este archivo como .env y completa los valores.
# El servidor no arranca correctamente si falta alguna de estas variables.

# Cadena de conexion de MongoDB (Atlas o local)
URI_MONGODB =

# Secretos de firma de tokens JWT.
# Genera cada uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ACCESS_TOKEN_SECRET =
REFRESH_TOKEN_SECRET =
ACTIVATION_TOKEN_SECRET =

# Cuenta de Gmail que envia los correos de verificacion.
# EMAIL_PASS NO es la clave normal de la cuenta: es una "contrasena de aplicacion"
# de 16 caracteres (sin espacios) que se genera en https://myaccount.google.com/apppasswords
# y que requiere tener activada la verificacion en 2 pasos.
EMAIL_USER =
EMAIL_PASS =

# URL base del frontend. Se usa para armar el enlace de activacion del correo.
# En desarrollo es el puerto de Vite; en produccion, el dominio publicado.
CLIENT_URI = http://localhost:5173
